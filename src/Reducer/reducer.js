// reducer.js

const initialStates = {

}

export const settingsReducer = (state = initialStates, action) => {
    switch (action.type) {
        // case "..." : 
        //     return {
        //         ...state,

        //     }

        default:
            return initialStates;
    }
};
